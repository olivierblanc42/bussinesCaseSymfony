{% extends 'back/base.html.twig' %}

{% block title %}Species index{% endblock %}

{% block body %}
    <h1>Species</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ species.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_back_species_index') }}">back to list</a>

    <a href="{{ path('app_back_species_edit', {'id': species.id}) }}">edit</a>

    {{ include('back/species/_delete_form.html.twig') }}
{% endblock %}
