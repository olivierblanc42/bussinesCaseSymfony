{% extends 'back/base.html.twig' %}

{% block title %}Brand{% endblock %}

{% block body %}
    <h1>Brand</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Label</th>
                <td>{{ brand.label }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_back_brand_index') }}">back to list</a>

    <a href="{{ path('app_back_brand_edit', {'id': brand.id}) }}">edit</a>

    {{ include('back/brand/_delete_form.html.twig') }}
{% endblock %}
